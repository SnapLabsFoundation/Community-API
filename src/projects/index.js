const updateMetadata = require('./updateMeta');
const accessMetadata = require('./accessMeta');
const updateProject = require('./updateProject');
const createProject = require('./createProject');

module.exports = {
    updateMetadata,
    accessMetadata,
    updateProject,
    createProject
};